Adicione aqui os ícones da extensão (16x16, 48x48 e 128x128 px) e referencie-os
em `manifest.json` em `action.default_icon` e `icons`. Sem eles, o Chrome usa
um ícone padrão — a extensão funciona normalmente sem isso.
